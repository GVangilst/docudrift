# Demo App

## 8.2. Bootstrap using the node command, avoid npm start

Best practice: prefer `node index.js` over relying on the start script.

See [Read more: avoid npm start](#82-bootstrap) and note you should not run
npm run build in production images.
