# Demo Library

## Quick Start

Generate an app with the CLI and run it:

```bash
npx create-demo /tmp/foo && cd /tmp/foo
```

Install dependencies:

```bash
npm install
```

Start the server:

```bash
npm start
```
